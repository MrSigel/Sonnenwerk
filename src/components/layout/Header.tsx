import { SiteNav } from "./SiteNav";

/**
 * Kopfbereich: sticky Premium-Navigation mit Logo, Abschnitts-Links
 * (Smooth-Scroll + Scrollspy) und CTA. Details in SiteNav.
 */
export function Header() {
  return <SiteNav />;
}
